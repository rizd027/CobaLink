/** Default app entry after sign-in (orders list). Keep middleware + redirects in sync with this. */
export const AUTH_LANDING_PATH = "/dashboard/orders";

/** All authenticated app routes live under this prefix. */
export const AUTH_PROTECTED_PREFIX = "/dashboard";
